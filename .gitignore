# variáveis de ambiente
.env
.env.local

# obs: config.js É versionado (só tem config pública do Supabase).
# A chave do Gemini fica na tabela app_settings do Supabase, nunca no repo.

# sistema
.DS_Store
Thumbs.db

# dependências (caso o projeto ganhe tooling no futuro)
node_modules/
